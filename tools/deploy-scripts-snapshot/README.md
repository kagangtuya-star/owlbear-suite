# Deploy script snapshots

The real scripts live one directory ABOVE this repo (next to `obr-suite/`,
`bestiary/`, `character-cards/` …), where there is no git repo, so changes to
them would otherwise leave no trace. These are read-only copies kept for
history — editing one here does nothing. Copy back up a level to apply.

Snapshotted 2026-08-25, after fixing the staging-directory bug that kept
re-publishing files the build had already dropped. See CHANGELOG.md.

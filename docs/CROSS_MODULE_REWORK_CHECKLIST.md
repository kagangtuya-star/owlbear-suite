# Owlbear Suite 跨模块改造清单

> 用途：换电脑后继续实施、测试和验收本轮跨模块改造。
>
> 来源：回退前备份 `obr-suite_backup_20260728/AI_CONTEXT/RUNBOOKS/2026-07-10-suite-bugfix-kickoff.md` 与 2026-07-27 dev 故障记录。
>
> 已按要求删除原第 10 项“Windows 图片加载工具说明”，以下重新连续编号。
>
> 实施基线：2026-08-20 已丢弃旧 dev 改动，改为从 stable `1.1.10`（提交 `810b2d6`）继续；禁止从旧 dev 或 `obr-suite_backup_20260728` 直接恢复代码。

## 总体纪律

- [ ] 先从当前稳定基线创建独立分支，不直接在主线做大规模实验。
- [ ] 按下面的批次顺序推进；一批验收通过后再进入下一批。
- [ ] 每项都必须留下错误日志、关键状态和复现路径，禁止吞异常。
- [ ] 类型检查、构建成功不等于功能完成；必须在真实 Owlbear 房间验证。
- [ ] 至少完成 DM + 两名玩家的多客户端测试，并覆盖明暗主题。
- [ ] dev 验收通过前不发布 stable；发现严重回归时停止后续批次。
- [ ] 不覆盖现有用户数据；涉及旧数据时必须明确迁移和回退方案。

## 第一批：数据正确性、竞态与基础稳定性

> **实施状态（2026-08-21）**：六项代码全部完成并通过 `tsc + vite build`；经 6 维度对抗性审查（16 项发现 → 14 项确认全部修复，含 4 个 major：sweep 与同步互斥、失败不得记入同步账本、预设合并幂等、回滚改为仅-ID 还原）。已部署：dev 频道（`1.0.141-dev`，提交 `9034607` 起）与角色卡服务器 `parser.py`（venv 11 单测全过、TS↔Python 摘要逐字节一致、健康检查 ok）。§4 已真实房间实测通过；其余各项复选框代表真实房间验收（DM+两玩家），实测通过前保持未勾。用户已放行第二批。
>
> 待用户拍板的产品决策：卡片 HP 模式下解除变身，token 血量恢复为变身前快照（现状）还是保留卡片当前值？现状下解除后与卡片短暂不同步，直到下一次卡片编辑。

### 1. 状态追踪性能与竞态

- [ ] 把 `OBR.scene.items.onChange` 当作完整场景快照处理，不重复读取全场。
- [ ] 合并短时间内的重复场景刷新，避免一次操作触发多次完整渲染。
- [ ] 同一个 Token 的状态写入必须串行，禁止并发互相覆盖。
- [ ] 替换相同稳定 ID 的气泡时，必须先确认旧气泡删除完成，再添加新气泡。
- [ ] 状态增删失败必须记录 Token ID、状态 ID、操作阶段和错误信息。
- [ ] 实测多人同时修改状态、快速增删同一状态、切换场景和重新加入房间。

### 2. 角色卡自动资源 ID

- [ ] 中文能力名生成的资源 ID 必须稳定且唯一，不能全部退化成 `auto-`。
- [ ] ID 应包含可读片段和稳定摘要；同一能力重复导入时保持一致。
- [ ] 绑定旧角色卡时检测空 ID、重复 ID，并提供可回退的兼容修复。
- [ ] 验证多个中文同名/近似名能力分别增减时不会串联。

### 3. 传送门首次移动

- [ ] 场景启动时为所有可拖动的 CHARACTER/MOUNT 建立坐标基线。
- [ ] 不依赖 selection watcher 异步预填旧坐标。
- [ ] 第一次拖动、启动时已选中、快速连续拖动都必须正确触发传送判断。
- [ ] 传送失败必须保留入口、目标、Token ID 和坐标日志。

### 4. 变身限制与兼容

> 2026-08-21 拍板：变身栈非空时 DM 的「变身」菜单也隐藏（与玩家一致）；DM 要改授权政策须先解除变身再开选择器。应用层拒绝守卫保留作兜底。

- [x] 变身栈非空时，UI 和底层写入都拒绝再次变身。（2026-08-21 实测：菜单隐藏 + 选怪拒绝通知）
- [ ] 竞态或绕过 UI 的调用也不能创建新的嵌套变身。
- [ ] 旧版本已经存在的嵌套栈仍可逐层解除，不能破坏旧数据。
- [ ] 测试角色卡 HP 与怪物 HP 两种计算模式及解除后的恢复结果。

### 5. 5etools 内容展示

- [ ] 识别 `spellcasting[].displayAs`。
- [ ] `action`、`bonus`、`reaction` 分别进入动作、附赠动作、反应区域。
- [ ] 怪物详情、全局搜索预览、Monster Studio 三条链路保持一致。
- [ ] 缺失或未知 `displayAs` 时明确记录并采用可解释的默认分类。

### 6. 角色卡武器编辑布局

- [ ] 五列 Grid 使用 `minmax(0, …)`，输入框允许收缩。
- [ ] 所有输入框设置合理的 `min-width: 0` 和宽度规则。
- [ ] 窄窗口、长武器名、中英文界面均不溢出。
- [ ] 验证第三个及后续输入框可点击、可输入，不被相邻元素覆盖。

## 第二批：权限、回合与投骰交互

### 7. 搜索栏仅 DM 可见

- [ ] 增加并持久化“仅 DM 可见”设置。
- [ ] 打开搜索栏的所有入口统一经过同一个权限函数。
- [ ] 玩家角色变化、场景 ready、场景切换和重新连接时都重新检查权限。
- [ ] 禁止出现先向玩家显示、随后再关闭的短暂泄漏。
- [ ] 权限判断或关闭失败时记录角色、场景状态和调用入口。

### 8. 先攻回合提示

- [ ] 当前玩家收到“你的回合”提示。
- [ ] 下一名公开玩家收到“做好准备”提示。
- [ ] 隐身单位不参与下一玩家计算，也不能通过提示时间泄露其数量或位置。
- [ ] 隐身角色的拥有者仍能收到自己的回合提示。
- [ ] 提示层必须穿透指针，不得遮黑画面或阻挡操作。
- [ ] 切换先攻失败时恢复本地乐观指针并输出完整错误，不得静默错位。

### 9. DM 固定投骰结果

- [ ] 仅 DM 可用，并在每个实际执行入口再次验证身份。
- [ ] 覆盖单个可投骰值、主骰盘、暗骰、单角色先攻劣势/普通/优势。
- [ ] 固定值必须是公式合法最小值到最大值之间的整数。
- [ ] 通过重新分配真实骰面得到目标总数，不能只篡改显示结果。
- [ ] 覆盖减法骰、重复修正、优势/劣势败者和 `max/min` 包装。
- [ ] 动画、历史记录、广播结果和最终总数必须一致。
- [ ] 群体豁免和群体先攻不得接入固定结果。

## 第三批：视觉系统

### 10. Owlbear 主题与整体视觉

- [ ] 从真实 `OBR.theme` 派生背景、纸张、文字和主色，不硬编码单一深色主题。
- [ ] 普通页面可以使用中性半透明表面；透明效果层必须保持真正透明。
- [ ] 明确排除骰子准星、资源提示、赞助层、回合特效等全屏透明 overlay，禁止被主题桥接涂黑。
- [ ] 检查所有顶层 iframe，但不要以“批量注入成功”代替逐页视觉验收。
- [ ] 拖拽预览、框选、传送门配对线使用低干扰、清晰可见的统一风格。
- [ ] 避免在高频更新或全屏透明层使用高成本 blur/filter。
- [ ] 验证明暗主题、窄窗口、缩放、触屏和所有全屏 modal/popover 的层级及指针行为。

## 本轮已经完成的前置修复

- [x] 默认怪物数据继续使用 `https://5e.kiwee.top`。
- [x] Suite 与旧独立 Bestiary 源码中的内部怪物图片地址改为 `https://5e.kiwee.top/img`。
- [x] Commoner 备用图片同步改到 kiwee，不再回到失效的 5e.tools 代理。
- [x] stable `1.1.10` 已发布，线上构建产物不再包含旧图片代理地址。
- [x] 设置 → 怪物图鉴新增「修复旧图片地址」一键按钮（仅 DM）：把当前场景 token 图片与变身快照中的 `obr.dnd.center/5etools-img` 前缀改写为 `https://5e.kiwee.top/img`，逐场景执行，自定义/外链图片不动（`src/modules/bestiary/repair-legacy-images.ts`）。含防重入守卫（修复中重渲染不会复活可点按钮）、先检查场景 ready 再弹确认框、控制台留 old→new 证据日志。
- [x] 世界包导入同步迁移：旧版导出的 `.fobr` 包原样携带失效代理地址，导入时（Pass-1）自动把 item 图片与变身快照里的旧前缀改写为 kiwee，避免导入旧存档后重新出现死图（`src/modules/worldPack/importer.ts`）。
- [ ] 在旧场景实测一键修复：修复后 token 纹理恢复、解除变身不再回退到失效地址、修复计数与控制台 evidence 日志一致；再导入一个迁移前导出的 `.fobr` 包验证图片正常。
- [x] 验证 kiwee 图片源有效（2026-08-20，用户确认按此标准验收）：Bat / Swarm of Bats / Hell Hound / Commoner 的 token URL 均返回 200 + `image/webp` + `Access-Control-Allow-Origin: *`，`search/index.json` 正常——满足 OBR WebGL 纹理加载的跨域要求。

## 最终验收

- [ ] TypeScript 类型检查通过。
- [ ] 生产构建通过，且产物中不再把 `obr.dnd.center/5etools-img` 用作图片来源（注意：settings chunk 会包含该字符串——那是「修复旧图片地址」按钮的匹配前缀和说明文案，属预期；验收时确认它只出现在 settings 产物里即可）。
- [ ] DM + 两名玩家完成状态、权限、先攻、投骰、变身和传送门回归。
- [ ] 明暗主题各检查一次主要面板和全部透明 overlay。
- [ ] 断网、上游 403、图片 404、广播失败和写入失败都有明确可见的错误证据。
- [ ] dev 连续稳定运行并完成玩家验收后，再决定是否发布 stable。
